// DO NOT EDIT.
// swift-format-ignore-file
// swiftlint:disable all
//
// Generated by the Swift generator plugin for the protocol buffer compiler.
// Source: realtime/v1/enums.proto
//
// For information on using the generated types, please see the documentation:
//   https://github.com/apple/swift-protobuf/

import SwiftProtobuf

// If the compiler emits an error on this type, it is because this file
// was generated by a version of the `protoc` Swift plug-in that is
// incompatible with the version of SwiftProtobuf to which you are linking.
// Please ensure that you are building against the same version of the API
// that was used to generate this file.
fileprivate nonisolated struct _GeneratedWithProtocGenSwiftVersion: SwiftProtobuf.ProtobufAPIVersionCheck {
  struct _2: SwiftProtobuf.ProtobufAPIVersion_2 {}
  typealias Version = _2
}

/// The multiplex channel CLASS a frame belongs to. A channel is a (class, key)
/// pair (see ChannelRef): the class selects the kind of stream, the key scopes it
/// to a specific subject. One physical WSS socket carries every class at once —
/// that multiplexing is the whole point of the plane (one socket per device).
///
/// Authorization is by ownership of the key (see ChannelRef): DM / NOTIFICATION /
/// PRESENCE keys MUST equal the connection's pinned identity; COUNTER / FEED keys
/// are public entity ids. The realtime plane performs no other authorization —
/// content visibility was decided upstream when the event was emitted.
public nonisolated enum Realtime_V1_ChannelClass: SwiftProtobuf.Enum, Swift.CaseIterable {
  public typealias RawValue = Int
  case unspecified // = 0

  /// Direct messages / chat. Key = the owning user id. At-least-once.
  case dm // = 1

  /// Notification + badge updates. Key = the owning user id. At-least-once.
  case notification // = 2

  /// Presence (online/away). Key = the owning user id. Fire-and-forget.
  case presence // = 3

  /// Live engagement magnitudes for an entity. Key = the entity id. Public,
  /// fire-and-forget (a dropped tick is superseded by the next).
  case counter // = 4

  /// Selective live feed signals. Key = the entity/author id. Fire-and-forget.
  case feed // = 5
  case UNRECOGNIZED(Int)

  public init() {
    self = .unspecified
  }

  public init?(rawValue: Int) {
    switch rawValue {
    case 0: self = .unspecified
    case 1: self = .dm
    case 2: self = .notification
    case 3: self = .presence
    case 4: self = .counter
    case 5: self = .feed
    default: self = .UNRECOGNIZED(rawValue)
    }
  }

  public var rawValue: Int {
    switch self {
    case .unspecified: return 0
    case .dm: return 1
    case .notification: return 2
    case .presence: return 3
    case .counter: return 4
    case .feed: return 5
    case .UNRECOGNIZED(let i): return i
    }
  }

  // The compiler won't synthesize support with the UNRECOGNIZED case.
  public static let allCases: [Realtime_V1_ChannelClass] = [
    .unspecified,
    .dm,
    .notification,
    .presence,
    .counter,
    .feed,
  ]

}

/// Server→client control directives, carried in a Control frame. These drive the
/// client's connection state machine; they are not domain events.
public nonisolated enum Realtime_V1_ServerControl: SwiftProtobuf.Enum, Swift.CaseIterable {
  public typealias RawValue = Int
  case unspecified // = 0

  /// Acknowledges a Subscribe for `channel`.
  case subscribed // = 1

  /// Acknowledges an Unsubscribe for `channel`.
  case unsubscribed // = 2

  /// The edge token is expiring/expired (RTM-1002). The client must silently
  /// refresh via `auth` and send an AuthRefresh (or re-handshake).
  case reauthRequired // = 3

  /// The node is draining on rollout (RTM-5003). The client must reconnect —
  /// elsewhere — after `reconnect_after_ms` PLUS its own jitter (the jitter is
  /// mandatory: it prevents a reconnect thundering-herd into the auth path).
  case reconnect // = 4

  /// The connection exceeded a limit (subscriptions / frame rate / size).
  case rateLimited // = 5

  /// A terminal or per-request error; carries a stable RTM-XXXX `code`.
  case error // = 6
  case UNRECOGNIZED(Int)

  public init() {
    self = .unspecified
  }

  public init?(rawValue: Int) {
    switch rawValue {
    case 0: self = .unspecified
    case 1: self = .subscribed
    case 2: self = .unsubscribed
    case 3: self = .reauthRequired
    case 4: self = .reconnect
    case 5: self = .rateLimited
    case 6: self = .error
    default: self = .UNRECOGNIZED(rawValue)
    }
  }

  public var rawValue: Int {
    switch self {
    case .unspecified: return 0
    case .subscribed: return 1
    case .unsubscribed: return 2
    case .reauthRequired: return 3
    case .reconnect: return 4
    case .rateLimited: return 5
    case .error: return 6
    case .UNRECOGNIZED(let i): return i
    }
  }

  // The compiler won't synthesize support with the UNRECOGNIZED case.
  public static let allCases: [Realtime_V1_ServerControl] = [
    .unspecified,
    .subscribed,
    .unsubscribed,
    .reauthRequired,
    .reconnect,
    .rateLimited,
    .error,
  ]

}

/// Why the gateway closed (or is about to close) a connection. Maps to the
/// RTM-1xxx / RTM-5xxx lifecycle codes so client telemetry can attribute churn.
public nonisolated enum Realtime_V1_CloseReason: SwiftProtobuf.Enum, Swift.CaseIterable {
  public typealias RawValue = Int
  case unspecified // = 0

  /// Graceful node drain on rollout (RTM-5003) — reconnect with backoff+jitter.
  case draining // = 1

  /// No heartbeat pong within the deadline; half-open connection reaped (RTM-5002).
  case heartbeatTimeout // = 2

  /// The per-connection send queue overflowed; slow consumer shed (RTM-5001).
  case sendQueueOverflow // = 3

  /// The session's edge token expired and was not refreshed (RTM-1002).
  case authExpired // = 4

  /// A transport/framing/protocol violation (RTM-2xxx).
  case protocolError // = 5
  case UNRECOGNIZED(Int)

  public init() {
    self = .unspecified
  }

  public init?(rawValue: Int) {
    switch rawValue {
    case 0: self = .unspecified
    case 1: self = .draining
    case 2: self = .heartbeatTimeout
    case 3: self = .sendQueueOverflow
    case 4: self = .authExpired
    case 5: self = .protocolError
    default: self = .UNRECOGNIZED(rawValue)
    }
  }

  public var rawValue: Int {
    switch self {
    case .unspecified: return 0
    case .draining: return 1
    case .heartbeatTimeout: return 2
    case .sendQueueOverflow: return 3
    case .authExpired: return 4
    case .protocolError: return 5
    case .UNRECOGNIZED(let i): return i
    }
  }

  // The compiler won't synthesize support with the UNRECOGNIZED case.
  public static let allCases: [Realtime_V1_CloseReason] = [
    .unspecified,
    .draining,
    .heartbeatTimeout,
    .sendQueueOverflow,
    .authExpired,
    .protocolError,
  ]

}

// MARK: - Code below here is support for the SwiftProtobuf runtime.

nonisolated extension Realtime_V1_ChannelClass: SwiftProtobuf._ProtoNameProviding {
  public static let _protobuf_nameMap = SwiftProtobuf._NameMap(bytecode: "\0\u{2}\0CHANNEL_CLASS_UNSPECIFIED\0\u{1}CHANNEL_CLASS_DM\0\u{1}CHANNEL_CLASS_NOTIFICATION\0\u{1}CHANNEL_CLASS_PRESENCE\0\u{1}CHANNEL_CLASS_COUNTER\0\u{1}CHANNEL_CLASS_FEED\0")
}

nonisolated extension Realtime_V1_ServerControl: SwiftProtobuf._ProtoNameProviding {
  public static let _protobuf_nameMap = SwiftProtobuf._NameMap(bytecode: "\0\u{2}\0SERVER_CONTROL_UNSPECIFIED\0\u{1}SERVER_CONTROL_SUBSCRIBED\0\u{1}SERVER_CONTROL_UNSUBSCRIBED\0\u{1}SERVER_CONTROL_REAUTH_REQUIRED\0\u{1}SERVER_CONTROL_RECONNECT\0\u{1}SERVER_CONTROL_RATE_LIMITED\0\u{1}SERVER_CONTROL_ERROR\0")
}

nonisolated extension Realtime_V1_CloseReason: SwiftProtobuf._ProtoNameProviding {
  public static let _protobuf_nameMap = SwiftProtobuf._NameMap(bytecode: "\0\u{2}\0CLOSE_REASON_UNSPECIFIED\0\u{1}CLOSE_REASON_DRAINING\0\u{1}CLOSE_REASON_HEARTBEAT_TIMEOUT\0\u{1}CLOSE_REASON_SEND_QUEUE_OVERFLOW\0\u{1}CLOSE_REASON_AUTH_EXPIRED\0\u{1}CLOSE_REASON_PROTOCOL_ERROR\0")
}
