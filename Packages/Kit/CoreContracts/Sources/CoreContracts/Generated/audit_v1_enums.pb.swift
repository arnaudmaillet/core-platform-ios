// DO NOT EDIT.
// swift-format-ignore-file
// swiftlint:disable all
//
// Generated by the Swift generator plugin for the protocol buffer compiler.
// Source: audit/v1/enums.proto
//
// For information on using the generated types, please see the documentation:
//   https://github.com/apple/swift-protobuf/

import SwiftProtobuf

// If the compiler emits an error on this type, it is because this file
// was generated by a version of the `protoc` Swift plug-in that is
// incompatible with the version of SwiftProtobuf to which you are linking.
// Please ensure that you are building against the same version of the API
// that was used to generate this file.
fileprivate nonisolated struct _GeneratedWithProtocGenSwiftVersion: SwiftProtobuf.ProtobufAPIVersionCheck {
  struct _2: SwiftProtobuf.ProtobufAPIVersion_2 {}
  typealias Version = _2
}

/// The compliance CATEGORY of an audit event. This is the primary classifier the
/// worker uses to resolve a retention policy and to derive the chain partition
/// (partitioning is hybrid: by tenant + category — the subject is carried as an
/// indexed field, NOT as the partition, so per-subject erasure never fragments
/// the chain). Categories are coarse and stable on purpose: they are an API and a
/// legal taxonomy, not a debugging facet.
public nonisolated enum Audit_V1_EventCategory: SwiftProtobuf.Enum, Swift.CaseIterable {
  public typealias RawValue = Int
  case unspecified // = 0

  /// Authentication lifecycle (login, token issuance, MFA, session end).
  case authentication // = 1

  /// Authorization decision (grant/deny of a protected operation).
  case authorization // = 2

  /// A trust & safety enforcement decision + its DSA statement-of-reasons.
  case moderation // = 3

  /// A consent / privacy-setting change (lawful-basis relevant).
  case consent // = 4

  /// Access to personal data (read/query of PII by an actor).
  case dataAccess // = 5

  /// A formal data export (e.g. DPO / regulator / subject-access request).
  case dataExport // = 6

  /// A data-erasure action (GDPR Art. 17 — recorded as a fact; the PII itself
  /// is destroyed by crypto-shred, not stored here).
  case dataErasure // = 7

  /// A privileged / administrative action (the sync fail-closed lane — see
  /// PrivilegedActionType).
  case privilegedAction // = 8

  /// A retention or legal-hold lifecycle event.
  case retention // = 9

  /// An account / identity lifecycle or identity-state event (account creation,
  /// activation, suspension, deletion, KYC status) — the subject's existence and
  /// standing, distinct from authentication (credentials) and authorization
  /// (permissions).
  case identity // = 10
  case UNRECOGNIZED(Int)

  public init() {
    self = .unspecified
  }

  public init?(rawValue: Int) {
    switch rawValue {
    case 0: self = .unspecified
    case 1: self = .authentication
    case 2: self = .authorization
    case 3: self = .moderation
    case 4: self = .consent
    case 5: self = .dataAccess
    case 6: self = .dataExport
    case 7: self = .dataErasure
    case 8: self = .privilegedAction
    case 9: self = .retention
    case 10: self = .identity
    default: self = .UNRECOGNIZED(rawValue)
    }
  }

  public var rawValue: Int {
    switch self {
    case .unspecified: return 0
    case .authentication: return 1
    case .authorization: return 2
    case .moderation: return 3
    case .consent: return 4
    case .dataAccess: return 5
    case .dataExport: return 6
    case .dataErasure: return 7
    case .privilegedAction: return 8
    case .retention: return 9
    case .identity: return 10
    case .UNRECOGNIZED(let i): return i
    }
  }

  // The compiler won't synthesize support with the UNRECOGNIZED case.
  public static let allCases: [Audit_V1_EventCategory] = [
    .unspecified,
    .authentication,
    .authorization,
    .moderation,
    .consent,
    .dataAccess,
    .dataExport,
    .dataErasure,
    .privilegedAction,
    .retention,
    .identity,
  ]

}

/// Who performed the action. Identities are pseudonymous (see Actor) — this only
/// classifies the KIND of principal so reports can separate human from machine
/// activity. The real identity↔pseudonym mapping lives in `account`, never here.
public nonisolated enum Audit_V1_ActorType: SwiftProtobuf.Enum, Swift.CaseIterable {
  public typealias RawValue = Int
  case unspecified // = 0

  /// An end user acting on their own/again another's data.
  case user // = 1

  /// A human operator / administrator (elevated privilege).
  case admin // = 2

  /// A fleet service acting on its own behalf (a service principal).
  case service // = 3

  /// An automated system process (scheduled job, the worker itself).
  case system // = 4
  case UNRECOGNIZED(Int)

  public init() {
    self = .unspecified
  }

  public init?(rawValue: Int) {
    switch rawValue {
    case 0: self = .unspecified
    case 1: self = .user
    case 2: self = .admin
    case 3: self = .service
    case 4: self = .system
    default: self = .UNRECOGNIZED(rawValue)
    }
  }

  public var rawValue: Int {
    switch self {
    case .unspecified: return 0
    case .user: return 1
    case .admin: return 2
    case .service: return 3
    case .system: return 4
    case .UNRECOGNIZED(let i): return i
    }
  }

  // The compiler won't synthesize support with the UNRECOGNIZED case.
  public static let allCases: [Audit_V1_ActorType] = [
    .unspecified,
    .user,
    .admin,
    .service,
    .system,
  ]

}

/// The narrow, enumerated set of actions that MUST be recorded synchronously and
/// fail CLOSED — the action is denied if it cannot be provably recorded first
/// (RecordPrivileged). LOCKED scope (see project_audit_service_blueprint): strictly
/// break-glass access + legal-hold lifecycle. Everything else — including consent
/// changes — flows on the async, fail-open lane. The enum is open so the enrolled
/// set can widen later WITHOUT a contract change; widening is a deliberate
/// coupling decision, never a default.
public nonisolated enum Audit_V1_PrivilegedActionType: SwiftProtobuf.Enum, Swift.CaseIterable {
  public typealias RawValue = Int
  case unspecified // = 0

  /// Emergency elevated access to data/systems outside normal authorization
  /// ("break glass"). Must be recorded before access is granted.
  case breakGlassAccess // = 1

  /// Placing a subject/dataset under legal hold (suspends erasure).
  case legalHoldPlace // = 2

  /// Releasing a legal hold (re-enables the retention/erasure lifecycle).
  case legalHoldRelease // = 3
  case UNRECOGNIZED(Int)

  public init() {
    self = .unspecified
  }

  public init?(rawValue: Int) {
    switch rawValue {
    case 0: self = .unspecified
    case 1: self = .breakGlassAccess
    case 2: self = .legalHoldPlace
    case 3: self = .legalHoldRelease
    default: self = .UNRECOGNIZED(rawValue)
    }
  }

  public var rawValue: Int {
    switch self {
    case .unspecified: return 0
    case .breakGlassAccess: return 1
    case .legalHoldPlace: return 2
    case .legalHoldRelease: return 3
    case .UNRECOGNIZED(let i): return i
    }
  }

  // The compiler won't synthesize support with the UNRECOGNIZED case.
  public static let allCases: [Audit_V1_PrivilegedActionType] = [
    .unspecified,
    .breakGlassAccess,
    .legalHoldPlace,
    .legalHoldRelease,
  ]

}

/// The outcome of the audited action. Kept generic so one envelope serves every
/// category: an authorization decision is PERMITTED/DENIED, a privileged action is
/// EXECUTED/FAILED. This is the "what happened" half of the record.
public nonisolated enum Audit_V1_Outcome: SwiftProtobuf.Enum, Swift.CaseIterable {
  public typealias RawValue = Int
  case unspecified // = 0
  case permitted // = 1
  case denied // = 2
  case executed // = 3
  case failed // = 4
  case UNRECOGNIZED(Int)

  public init() {
    self = .unspecified
  }

  public init?(rawValue: Int) {
    switch rawValue {
    case 0: self = .unspecified
    case 1: self = .permitted
    case 2: self = .denied
    case 3: self = .executed
    case 4: self = .failed
    default: self = .UNRECOGNIZED(rawValue)
    }
  }

  public var rawValue: Int {
    switch self {
    case .unspecified: return 0
    case .permitted: return 1
    case .denied: return 2
    case .executed: return 3
    case .failed: return 4
    case .UNRECOGNIZED(let i): return i
    }
  }

  // The compiler won't synthesize support with the UNRECOGNIZED case.
  public static let allCases: [Audit_V1_Outcome] = [
    .unspecified,
    .permitted,
    .denied,
    .executed,
    .failed,
  ]

}

/// The GDPR Art. 6 lawful basis under which the personal-data processing in the
/// event was carried out. Required on PII-touching categories so the trail is
/// self-describing for an accountability (Art. 5(2)) review.
public nonisolated enum Audit_V1_LawfulBasis: SwiftProtobuf.Enum, Swift.CaseIterable {
  public typealias RawValue = Int
  case unspecified // = 0
  case consent // = 1
  case contract // = 2
  case legalObligation // = 3
  case vitalInterests // = 4
  case publicTask // = 5
  case legitimateInterests // = 6
  case UNRECOGNIZED(Int)

  public init() {
    self = .unspecified
  }

  public init?(rawValue: Int) {
    switch rawValue {
    case 0: self = .unspecified
    case 1: self = .consent
    case 2: self = .contract
    case 3: self = .legalObligation
    case 4: self = .vitalInterests
    case 5: self = .publicTask
    case 6: self = .legitimateInterests
    default: self = .UNRECOGNIZED(rawValue)
    }
  }

  public var rawValue: Int {
    switch self {
    case .unspecified: return 0
    case .consent: return 1
    case .contract: return 2
    case .legalObligation: return 3
    case .vitalInterests: return 4
    case .publicTask: return 5
    case .legitimateInterests: return 6
    case .UNRECOGNIZED(let i): return i
    }
  }

  // The compiler won't synthesize support with the UNRECOGNIZED case.
  public static let allCases: [Audit_V1_LawfulBasis] = [
    .unspecified,
    .consent,
    .contract,
    .legalObligation,
    .vitalInterests,
    .publicTask,
    .legitimateInterests,
  ]

}

/// The result of a ledger integrity verification over a partition range
/// (VerifyIntegrity). Anything other than VERIFIED is a tamper/truncation signal
/// that must alarm — never be silently repaired (maps to AUD-2xxx).
public nonisolated enum Audit_V1_IntegrityStatus: SwiftProtobuf.Enum, Swift.CaseIterable {
  public typealias RawValue = Int
  case unspecified // = 0

  /// The recomputed hash chain matches and agrees with the anchored checkpoint.
  case verified // = 1

  /// A recomputed record hash != the stored hash (AUD-2001) — tampering.
  case hashMismatch // = 2

  /// A hole in the monotonic per-partition sequence (AUD-2002) — truncation.
  case sequenceGap // = 3

  /// A signed checkpoint disagrees with the external witness (AUD-2004) —
  /// operator-level tampering past the database role.
  case checkpointDivergence // = 4
  case UNRECOGNIZED(Int)

  public init() {
    self = .unspecified
  }

  public init?(rawValue: Int) {
    switch rawValue {
    case 0: self = .unspecified
    case 1: self = .verified
    case 2: self = .hashMismatch
    case 3: self = .sequenceGap
    case 4: self = .checkpointDivergence
    default: self = .UNRECOGNIZED(rawValue)
    }
  }

  public var rawValue: Int {
    switch self {
    case .unspecified: return 0
    case .verified: return 1
    case .hashMismatch: return 2
    case .sequenceGap: return 3
    case .checkpointDivergence: return 4
    case .UNRECOGNIZED(let i): return i
    }
  }

  // The compiler won't synthesize support with the UNRECOGNIZED case.
  public static let allCases: [Audit_V1_IntegrityStatus] = [
    .unspecified,
    .verified,
    .hashMismatch,
    .sequenceGap,
    .checkpointDivergence,
  ]

}

// MARK: - Code below here is support for the SwiftProtobuf runtime.

nonisolated extension Audit_V1_EventCategory: SwiftProtobuf._ProtoNameProviding {
  public static let _protobuf_nameMap = SwiftProtobuf._NameMap(bytecode: "\0\u{2}\0EVENT_CATEGORY_UNSPECIFIED\0\u{1}EVENT_CATEGORY_AUTHENTICATION\0\u{1}EVENT_CATEGORY_AUTHORIZATION\0\u{1}EVENT_CATEGORY_MODERATION\0\u{1}EVENT_CATEGORY_CONSENT\0\u{1}EVENT_CATEGORY_DATA_ACCESS\0\u{1}EVENT_CATEGORY_DATA_EXPORT\0\u{1}EVENT_CATEGORY_DATA_ERASURE\0\u{1}EVENT_CATEGORY_PRIVILEGED_ACTION\0\u{1}EVENT_CATEGORY_RETENTION\0\u{1}EVENT_CATEGORY_IDENTITY\0")
}

nonisolated extension Audit_V1_ActorType: SwiftProtobuf._ProtoNameProviding {
  public static let _protobuf_nameMap = SwiftProtobuf._NameMap(bytecode: "\0\u{2}\0ACTOR_TYPE_UNSPECIFIED\0\u{1}ACTOR_TYPE_USER\0\u{1}ACTOR_TYPE_ADMIN\0\u{1}ACTOR_TYPE_SERVICE\0\u{1}ACTOR_TYPE_SYSTEM\0")
}

nonisolated extension Audit_V1_PrivilegedActionType: SwiftProtobuf._ProtoNameProviding {
  public static let _protobuf_nameMap = SwiftProtobuf._NameMap(bytecode: "\0\u{2}\0PRIVILEGED_ACTION_TYPE_UNSPECIFIED\0\u{1}PRIVILEGED_ACTION_TYPE_BREAK_GLASS_ACCESS\0\u{1}PRIVILEGED_ACTION_TYPE_LEGAL_HOLD_PLACE\0\u{1}PRIVILEGED_ACTION_TYPE_LEGAL_HOLD_RELEASE\0")
}

nonisolated extension Audit_V1_Outcome: SwiftProtobuf._ProtoNameProviding {
  public static let _protobuf_nameMap = SwiftProtobuf._NameMap(bytecode: "\0\u{2}\0OUTCOME_UNSPECIFIED\0\u{1}OUTCOME_PERMITTED\0\u{1}OUTCOME_DENIED\0\u{1}OUTCOME_EXECUTED\0\u{1}OUTCOME_FAILED\0")
}

nonisolated extension Audit_V1_LawfulBasis: SwiftProtobuf._ProtoNameProviding {
  public static let _protobuf_nameMap = SwiftProtobuf._NameMap(bytecode: "\0\u{2}\0LAWFUL_BASIS_UNSPECIFIED\0\u{1}LAWFUL_BASIS_CONSENT\0\u{1}LAWFUL_BASIS_CONTRACT\0\u{1}LAWFUL_BASIS_LEGAL_OBLIGATION\0\u{1}LAWFUL_BASIS_VITAL_INTERESTS\0\u{1}LAWFUL_BASIS_PUBLIC_TASK\0\u{1}LAWFUL_BASIS_LEGITIMATE_INTERESTS\0")
}

nonisolated extension Audit_V1_IntegrityStatus: SwiftProtobuf._ProtoNameProviding {
  public static let _protobuf_nameMap = SwiftProtobuf._NameMap(bytecode: "\0\u{2}\0INTEGRITY_STATUS_UNSPECIFIED\0\u{1}INTEGRITY_STATUS_VERIFIED\0\u{1}INTEGRITY_STATUS_HASH_MISMATCH\0\u{1}INTEGRITY_STATUS_SEQUENCE_GAP\0\u{1}INTEGRITY_STATUS_CHECKPOINT_DIVERGENCE\0")
}
